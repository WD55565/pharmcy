# Presentation Providers

Riverpod providers (state notifiers / async notifiers built with
`riverpod_generator`) that call use cases and expose `AsyncValue`-driven
state to screens and widgets.

No implementation yet — this is architecture scaffolding only.
