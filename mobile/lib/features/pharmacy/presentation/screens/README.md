# Screens

Route-level widgets registered in `app/router/app_router.dart` (e.g. a
pharmacy list screen, detail screen). Consume `presentation/providers` and
render `core/widgets`' `LoadingView` / `ErrorView` / `EmptyView` for the
respective async states.

No implementation yet — this is architecture scaffolding only.
