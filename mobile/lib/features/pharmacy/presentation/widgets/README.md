# Feature Widgets

Widgets specific to the pharmacy feature (e.g. a pharmacy list tile, a duty
status badge) that aren't generic enough to belong in `core/widgets`.

No implementation yet — this is architecture scaffolding only.
