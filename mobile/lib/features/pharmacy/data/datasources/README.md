# Data Sources

Remote/local data sources for the pharmacy feature (e.g. a `PharmacyRemoteDataSource`
that calls the backend via the shared `dio` provider from `core/network`). Returns raw
API models, never domain entities.

No implementation yet — this is architecture scaffolding only.
