# Repository Implementations

Concrete implementations of the interfaces declared in `domain/repositories`.
Combine a data source with model-to-entity mapping and return `core/network`'s
`Result<T>` so the domain/presentation layers never see Dio or JSON directly.

No implementation yet — this is architecture scaffolding only.
