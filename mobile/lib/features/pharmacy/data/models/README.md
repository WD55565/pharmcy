# Data Models

Freezed + json_serializable DTOs that mirror the backend's
`PharmacyResponse`/`PharmacyRequest` JSON shape (see
`backend/src/main/java/com/nobetcieczaneplus/dto`). Mapped to/from domain
entities in `data/repositories`, never used directly by the presentation layer.

No implementation yet — this is architecture scaffolding only.
