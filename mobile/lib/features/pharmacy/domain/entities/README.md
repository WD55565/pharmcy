# Domain Entities

Plain, backend-agnostic representations of a pharmacy used throughout the app
(e.g. `Pharmacy`). No JSON annotations, no Dio, no framework dependencies —
just the shape of the business data.

No implementation yet — this is architecture scaffolding only.
