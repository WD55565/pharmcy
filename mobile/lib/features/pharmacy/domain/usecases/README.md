# Use Cases

Single-purpose application operations (e.g. `GetNearbyPharmacies`,
`SearchPharmaciesByDistrict`) that call one or more repository methods.
Keeps business rules out of Riverpod providers and widgets.

No implementation yet — this is architecture scaffolding only.
